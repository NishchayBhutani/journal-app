spring:
  application:
    name: Journal App
  datasource:
    url: jdbc:postgresql://localhost:5432/journaldb
    username: postgres
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true